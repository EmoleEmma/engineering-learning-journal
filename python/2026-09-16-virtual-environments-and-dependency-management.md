# Virtual Environments & Dependency Management

**Date:** September 16, 2026

## What I Learned

Today I learned how Python environments and dependency management help keep project dependencies isolated and organized.

I learned about:

- Python environments
- Virtual environments
- `venv`
- `virtualenv`
- `pip`
- Dependency management
- Package installation
- Requirements files
- Project configuration
- Python version management
- `pyenv`
- Pipenv
- Poetry
- `pyproject.toml`

## What I Practiced

I created a Python virtual environment using `venv` and practiced activating and deactivating the environment.

I also practiced installing Python packages with `pip` and recording project dependencies in a requirements file.

I experimented with project configuration and learned how dependency-management tools can be used to organize Python projects.

## What I Understand

I now understand why virtual environments are important when working on Python projects.

A virtual environment allows a project to have its own installed dependencies without interfering with packages used by other Python projects.

I also understand that dependency files help document the packages required to run a project.

## What I Still Find Difficult

I need more practice with:

- Choosing the appropriate dependency-management approach
- Understanding the differences between `venv` and `virtualenv`
- Managing project dependencies
- `pyproject.toml`
- Poetry
- Pipenv
- Python version management with `pyenv`
- Reproducing an environment on another computer

## Practical Exercise

I created a small Python project inside a virtual environment, installed an external package using `pip`, and documented the project's dependencies.

[View Exercise](../small-projects/python/Dependency-Management-Demo.py)

## Next Step

Next I will begin reviewing the Python topics I have learned and apply them together in a larger practical project.
